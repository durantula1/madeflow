"use client";
import { useState } from "react";
import { approveVersionAction, requestChangesAction } from "@/modules/portal/actions";

export function ReviewActions({ token, customerName, customerEmail }: { token: string; customerName: string; customerEmail: string | null }) {
  const [mode, setMode] = useState<"approve"|"review"|null>(null); const input = "mt-1.5 h-10 w-full rounded-xl border bg-background px-3 outline-none focus:border-primary focus:ring-3 focus:ring-primary/15";
  return <section className="rounded-2xl border bg-card p-5 shadow-sm"><h2 className="font-semibold">Твоето решение</h2><p className="mt-1 text-sm text-muted-foreground">Одобрението се записва към точно тази версия и нейния цифров отпечатък.</p><div className="mt-5 grid gap-3 sm:grid-cols-2"><button onClick={() => setMode("approve")} className="rounded-xl bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground">Одобрявам версията</button><button onClick={() => setMode("review")} className="rounded-xl border px-4 py-3 text-sm font-semibold">Имам коментар / промени</button></div>{mode === "approve" && <form action={approveVersionAction} className="mt-5 space-y-4 rounded-xl bg-muted p-4"><input type="hidden" name="token" value={token} /><label className="block text-sm font-medium">Име<input name="approverName" required defaultValue={customerName} className={input} /></label><label className="block text-sm font-medium">Имейл<input name="approverEmail" type="email" defaultValue={customerEmail ?? ""} className={input} /></label><label className="flex items-start gap-3 text-sm"><input type="checkbox" name="confirmation" required className="mt-1 size-4 accent-primary" /><span>Потвърждавам, че прегледах спецификацията, офертата и файловете и одобрявам тази версия.</span></label><button className="rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground">Потвърди одобрението</button></form>}{mode === "review" && <form action={requestChangesAction} className="mt-5 space-y-4 rounded-xl bg-muted p-4"><input type="hidden" name="token" value={token} /><input type="hidden" name="customerName" value={customerName} /><label className="block text-sm font-medium">Вид<select name="state" className={input}><option value="changes_requested">Искам промени</option><option value="comment">Само коментар</option></select></label><label className="block text-sm font-medium">Съобщение<textarea name="message" required rows={4} className="mt-1.5 w-full rounded-xl border bg-background p-3 outline-none focus:border-primary" /></label><button className="rounded-xl bg-foreground px-4 py-2.5 text-sm font-semibold text-background">Изпрати</button></form>}</section>;
}
