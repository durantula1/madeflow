import { CheckCircle2, CreditCard, Hammer, Shield, Wrench } from "lucide-react";
import { formatMoneyFromMinor } from "@/lib/money";
import { formatDate, orderStageLabels } from "@/lib/presentation/orders";
import { addWarrantyItemAction, completeInstallationAction, createServiceRequestAction, recordPaymentAction, scheduleInstallationAction, updateOrderStageAction } from "@/modules/operations/actions";

type Payment={id:string;kind:string;amountMinor:bigint;currency:string;paidAt:Date;note:string|null}; type Installation={scheduledFor:Date|null;installedAt:Date|null;acceptanceStatus:string;notes:string|null}|null; type Warranty={id:string;name:string;manufacturer:string|null;model:string|null;serialNumber:string|null;warrantyStart:string|null;warrantyEnd:string|null}; type Service={id:string;title:string;status:string;description:string};
export function OperationsPanel({orderId,currency,stage,payments,installation,warranties,services}:{orderId:string;currency:string;stage:string;payments:Payment[];installation:Installation;warranties:Warranty[];services:Service[]}){const input="mt-1 h-9 w-full rounded-lg border bg-background px-2 text-sm";return <div className="space-y-6">
  <section className="rounded-2xl border bg-card p-5 shadow-sm"><div className="flex items-center gap-2"><CheckCircle2 className="size-5 text-primary"/><h2 className="font-semibold">Етап на поръчката</h2></div><form action={updateOrderStageAction} className="mt-4 flex gap-2"><input type="hidden" name="orderId" value={orderId}/><select name="stage" defaultValue={stage} className="h-10 flex-1 rounded-xl border bg-background px-3 text-sm">{Object.entries(orderStageLabels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select><button className="rounded-xl bg-foreground px-4 text-sm font-semibold text-background">Запази</button></form></section>
  <section className="rounded-2xl border bg-card p-5 shadow-sm"><div className="flex items-center gap-2"><CreditCard className="size-5 text-primary"/><h2 className="font-semibold">Плащания</h2></div><div className="mt-4 space-y-2">{payments.map(payment=><div key={payment.id} className="flex justify-between rounded-xl bg-muted p-3 text-sm"><span>{payment.kind==="deposit"?"Капаро":payment.kind==="final"?"Финално":payment.kind==="progress"?"Междинно":"Друго"}<small className="ml-2 text-muted-foreground">{formatDate(payment.paidAt)}</small></span><strong>{formatMoneyFromMinor(payment.amountMinor,payment.currency)}</strong></div>)}</div><form action={recordPaymentAction} className="mt-4 grid gap-3 border-t pt-4 sm:grid-cols-4"><input type="hidden" name="orderId" value={orderId}/><input type="hidden" name="currency" value={currency}/><label className="text-xs text-muted-foreground">Вид<select name="kind" className={input}><option value="deposit">Капаро</option><option value="progress">Междинно</option><option value="final">Финално</option><option value="other">Друго</option></select></label><label className="text-xs text-muted-foreground">Сума<input name="amount" required inputMode="decimal" className={input}/></label><label className="text-xs text-muted-foreground sm:col-span-2">Бележка<input name="note" className={input}/></label><button className="h-9 rounded-lg bg-primary px-3 text-sm font-semibold text-primary-foreground sm:col-start-4">Запиши</button></form></section>
  <section className="rounded-2xl border bg-card p-5 shadow-sm"><div className="flex items-center gap-2"><Hammer className="size-5 text-primary"/><h2 className="font-semibold">Монтаж</h2></div>{installation&&<p className="mt-3 text-sm text-muted-foreground">Насрочен: {formatDate(installation.scheduledFor)} · статус: {installation.acceptanceStatus}</p>}<form action={scheduleInstallationAction} className="mt-4 grid gap-3 sm:grid-cols-[220px_1fr_auto]"><input type="hidden" name="orderId" value={orderId}/><label className="text-xs text-muted-foreground">Дата и час<input name="scheduledFor" type="datetime-local" required className={input}/></label><label className="text-xs text-muted-foreground">Бележки<input name="notes" className={input}/></label><button className="mt-5 h-9 rounded-lg border px-3 text-sm font-semibold">Насрочи</button></form>{installation&&<form action={completeInstallationAction} className="mt-3 flex justify-end gap-2"><input type="hidden" name="orderId" value={orderId}/><select name="acceptanceStatus" className="h-9 rounded-lg border bg-background px-2 text-sm"><option value="accepted">Приет без забележки</option><option value="issues">Има забележки</option></select><button className="h-9 rounded-lg bg-foreground px-3 text-sm font-semibold text-background">Завърши монтажа</button></form>}</section>
  <section className="rounded-2xl border bg-card p-5 shadow-sm"><div className="flex items-center gap-2"><Shield className="size-5 text-primary"/><h2 className="font-semibold">Гаранции</h2></div><div className="mt-4 grid gap-2 sm:grid-cols-2">{warranties.map(item=><div key={item.id} className="rounded-xl border p-3 text-sm"><p className="font-medium">{item.name}</p><p className="mt-1 text-xs text-muted-foreground">{[item.manufacturer,item.model,item.serialNumber].filter(Boolean).join(" · ")||"Без допълнителни данни"}</p><p className="mt-2 text-xs">{item.warrantyStart||"—"} → {item.warrantyEnd||"—"}</p></div>)}</div><form action={addWarrantyItemAction} className="mt-4 grid gap-3 border-t pt-4 sm:grid-cols-3"><input type="hidden" name="orderId" value={orderId}/><label className="text-xs text-muted-foreground">Артикул<input name="name" required className={input}/></label><label className="text-xs text-muted-foreground">Производител<input name="manufacturer" className={input}/></label><label className="text-xs text-muted-foreground">Модел<input name="model" className={input}/></label><label className="text-xs text-muted-foreground">Сериен №<input name="serialNumber" className={input}/></label><label className="text-xs text-muted-foreground">Начало<input name="warrantyStart" type="date" className={input}/></label><label className="text-xs text-muted-foreground">Край<input name="warrantyEnd" type="date" className={input}/></label><button className="h-9 rounded-lg bg-primary px-3 text-sm font-semibold text-primary-foreground sm:col-start-3">Добави гаранция</button></form></section>
  <section className="rounded-2xl border bg-card p-5 shadow-sm"><div className="flex items-center gap-2"><Wrench className="size-5 text-primary"/><h2 className="font-semibold">Сервизни случаи</h2></div><div className="mt-4 space-y-2">{services.map(item=><div key={item.id} className="rounded-xl border p-3 text-sm"><div className="flex justify-between"><strong>{item.title}</strong><span className="text-xs text-muted-foreground">{item.status}</span></div><p className="mt-1 text-muted-foreground">{item.description}</p></div>)}</div><form action={createServiceRequestAction} className="mt-4 grid gap-3 border-t pt-4 sm:grid-cols-2"><input type="hidden" name="orderId" value={orderId}/><label className="text-xs text-muted-foreground">Заглавие<input name="title" required className={input}/></label><label className="text-xs text-muted-foreground">Гаранционен артикул<select name="warrantyItemId" className={input}><option value="">Без връзка</option>{warranties.map(item=><option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="text-xs text-muted-foreground sm:col-span-2">Описание<textarea name="description" required rows={3} className="mt-1 w-full rounded-lg border bg-background p-2 text-sm"/></label><button className="h-9 rounded-lg bg-foreground px-3 text-sm font-semibold text-background sm:col-start-2">Отвори случай</button></form></section>
  </div>}
