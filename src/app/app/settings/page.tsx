import { Building2, Layers3, Users } from "lucide-react";
import { requireTenantContext } from "@/lib/authz/tenant-context";
import { updateOrganizationAction } from "@/modules/organizations/actions";
import { getOrganizationSettings } from "@/modules/organizations/queries";
export default async function SettingsPage(){const context=await requireTenantContext();const{organization,members,templates}=await getOrganizationSettings(context.organizationId);if(!organization)return null;const input="mt-1.5 h-10 w-full rounded-xl border bg-background px-3 text-sm";return <><p className="text-sm font-semibold text-primary">Workspace</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Настройки</h1><p className="mt-2 text-muted-foreground">Фирмени данни, екип и работни шаблони.</p><div className="mt-7 grid gap-6 xl:grid-cols-2"><section className="rounded-2xl border bg-card p-6 shadow-sm"><div className="flex items-center gap-2"><Building2 className="size-5 text-primary"/><h2 className="font-semibold">Организация</h2></div><form action={updateOrganizationAction} className="mt-5 grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium sm:col-span-2">Име<input name="name" defaultValue={organization.name} className={input}/></label><label className="text-sm font-medium">Префикс на поръчките<input name="orderNumberPrefix" defaultValue={organization.orderNumberPrefix} className={input}/></label><label className="text-sm font-medium">Валута<select name="defaultCurrency" defaultValue={organization.defaultCurrency} className={input}><option value="EUR">EUR</option><option value="BGN">BGN</option></select></label><label className="text-sm font-medium">Цвят на марката<input name="brandColor" defaultValue={organization.brandColor??""} placeholder="#397047" className={input}/></label><button className="mt-6 h-10 rounded-xl bg-primary px-4 text-sm font-semibold text-primary-foreground">Запази</button></form></section><section className="rounded-2xl border bg-card p-6 shadow-sm"><div className="flex items-center gap-2"><Users className="size-5 text-primary"/><h2 className="font-semibold">Екип</h2></div><div className="mt-5 divide-y">{members.map(member=><div key={member.userId} className="flex items-center justify-between py-3"><div><p className="text-sm font-medium">{member.displayName??"Член на екипа"}</p><p className="font-mono text-[10px] text-muted-foreground">{member.userId}</p></div><span className="rounded-full bg-muted px-2 py-1 text-xs">{member.role}</span></div>)}</div><p className="mt-5 rounded-xl bg-muted p-3 text-xs text-muted-foreground">Поканите за допълнителни членове ще бъдат активирани след бета достъпа. Моделът и ролите са готови.</p></section><section className="rounded-2xl border bg-card p-6 shadow-sm xl:col-span-2"><div className="flex items-center gap-2"><Layers3 className="size-5 text-primary"/><h2 className="font-semibold">Шаблони</h2></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{templates.map(template=><div key={template.id} className="rounded-xl border p-4"><div className="flex justify-between"><p className="font-medium">{template.name}</p><span className="text-xs text-muted-foreground">v{template.version}</span></div><p className="mt-1 text-xs text-muted-foreground">{template.scope==="platform"?"MadeFlow шаблон":"Фирмен шаблон"}</p></div>)}</div></section></div></>}
